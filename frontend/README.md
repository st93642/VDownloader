# VDownloader Frontend

This directory will contain the client-side application for VDownloader. The initial scope of this ticket is focused on the backend scaffold, so the frontend portion is intentionally left minimal. Future tickets can leverage this folder for a React, Next.js, or other SPA implementation.
