# Docker production environment configuration
# Copy this file to .env when deploying with Docker

# Application settings
NODE_ENV=production
PORT=4000
APP_BASE_URL=http://localhost:4000

# Download settings
DOWNLOAD_TEMP_DIR=.tmp/downloads
MAX_CONCURRENT_JOBS=2

# Rate limiting configuration
# These values are used internally and don't need to be changed unless customizing
